# Named `logfire_variables`, not `logfire`, on purpose: a `tests/logfire/`
# package would shadow the installed `logfire` library for `import logfire`
# inside these tests. Do not rename to mirror the source package.
